# ArtCollab Recommender con SQLite

Sistema de recomendación de artistas usando FastAPI + SQLite + IA local.

## Instalación
pip install -r requirements.txt

## Ejecutar
uvicorn app.main:app --reload

Visita:
http://127.0.0.1:8000/docs
